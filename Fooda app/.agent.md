# Workspace notes
- <project is chinese,use chines to make>
- <powershell command always use english command>
- <All PHP files not used in the project should be named as test_*.*, such as for testing or verification purposes.>
- <eveloping the features using PHP and CSS, but they will eventually be migrated to Flutter for mobile app deployment, so please ensure everything is compatible with Flutter.>

> I am currently developing the features using PHP and CSS, but they will eventually be migrated to Flutter for mobile app deployment, so please ensure everything is compatible with Flutter.
